# 程式設計與資料結構：期中實作考試

## 考試說明

- 本考試共有 12 題，全部必做，總分 120 分。
- 每題 10 分。
- 可以查閱課程講義、Java 官方文件與網路資料。
- 最後繳交的程式必須符合指定檔名、`public class` 名稱及 method 宣告。
- 每題會使用未公開資料測試一般案例與邊界條件。
- 不可將範例輸出或測試答案直接寫死在程式中。
- 除非題目明確允許，不可修改指定 method 的名稱、參數及回傳型態。
- 可新增 `private` helper method、constructor 或必要的 package-private class。
- 僅有畫面截圖、空檔案或無法編譯的程式，不視為完成。

## 繳交結構

請在 repository 建立 `midterm_exam` 資料夾，剛好放入以下 12 個 Java 檔案：

```text
midterm_exam/
├── Q01_InventoryItem.java
├── Q02_ServiceOrder.java
├── Q03_EmployeePayroll.java
├── Q04_NotificationRouter.java
├── Q05_BoundedBox.java
├── Q06_EnrollmentIndex.java
├── Q07_RequestPipeline.java
├── Q08_RecursiveAudit.java
├── Q09_TreeTraversal.java
├── Q10_BstDirectory.java
├── Q11_BstDeletion.java
└── Q12_StudentBstSystem.java
```

需要的輔助類別請寫在同一題指定的 `.java` 檔案中，不要另外增加繳交檔案。

---

## 第 1 題：封裝的庫存項目

配分：10 分  
指定檔名：`Q01_InventoryItem.java`  
指定類別：`Q01_InventoryItem`

### 情境

庫存項目的編號與名稱建立後不可更換，庫存量只能透過指定 method 修改，不能從類別外直接指定負數。

### 必要欄位與 Method

```java
public class Q01_InventoryItem {
    private final String id;
    private final String name;
    private int stock;

    public Q01_InventoryItem(String id, String name, int stock)
    public String getId()
    public String getName()
    public int getStock()
    public boolean restock(int amount)
    public boolean sell(int amount)
    public String status()
}
```

### 功能要求

1. `id` 或 `name` 為 `null`、去除前後空白後為空字串時，constructor 必須丟出 `IllegalArgumentException`。
2. Constructor 的 `stock` 小於 0 時以 0 儲存。
3. `id` 與 `name` 儲存去除前後空白後的內容。
4. `restock(amount)` 只有在 `amount > 0` 時增加庫存並回傳 `true`，否則不修改並回傳 `false`。
5. `sell(amount)` 只有在 `amount > 0` 且庫存足夠時扣除並回傳 `true`，否則不修改並回傳 `false`。
6. `status()` 格式必須為 `id|name|stock`。

### 範例

```java
Q01_InventoryItem item = new Q01_InventoryItem(" P100 ", " Keyboard ", 5);
System.out.println(item.restock(3));
System.out.println(item.sell(6));
System.out.println(item.sell(3));
System.out.println(item.status());
```

```text
true
true
false
P100|Keyboard|2
```


### 配分

- 欄位封裝與 constructor validation：3 分。
- `restock()`：2 分。
- `sell()`：3 分。
- Getter 與 `status()`：2 分。

---

## 第 2 題：Composition 維修訂單

配分：10 分  
指定檔名：`Q02_ServiceOrder.java`  
指定類別：`Q02_ServiceOrder`

### 情境

一張維修訂單由多個維修項目組成。每個項目包含項目名稱、單價及數量，訂單負責保存項目並計算總額。

### 必要結構

```java
public class Q02_ServiceOrder {
    public static class LineItem {
        public LineItem(String name, int unitPrice, int quantity)
        public String getName()
        public int getUnitPrice()
        public int getQuantity()
        public int subtotal()
    }

    public Q02_ServiceOrder(String orderId)
    public boolean addItem(String name, int unitPrice, int quantity)
    public int itemCount()
    public int totalAmount()
    public String largestItemName()
    public java.util.List<String> itemSummaries()
}
```

### 功能要求

1. `orderId` 不得為 `null` 或 blank，否則丟出 `IllegalArgumentException`。
2. 項目名稱不得為 `null` 或 blank；`unitPrice` 不得小於 0；`quantity` 必須大於 0。不合法時 `addItem()` 回傳 `false`。
3. `subtotal()` 等於單價乘以數量。
4. `totalAmount()` 回傳所有項目小計總和。
5. `largestItemName()` 回傳小計最高的項目名稱；平手回傳較早加入者；沒有項目時回傳空字串。
6. `itemSummaries()` 依加入順序回傳 `名稱:小計`，而且 caller 不得透過回傳的 List 修改訂單內部資料。

### 範例

```java
Q02_ServiceOrder order = new Q02_ServiceOrder("R-01");
order.addItem("Inspection", 300, 1);
order.addItem("Cable", 80, 4);
order.addItem("Cleaning", 200, 1);
System.out.println(order.itemCount());
System.out.println(order.totalAmount());
System.out.println(order.largestItemName());
System.out.println(order.itemSummaries());
```

```text
3
820
Cable
[Inspection:300, Cable:320, Cleaning:200]
```


### 配分

- `LineItem` 封裝與小計：2 分。
- 新增驗證與 composition：3 分。
- 總額及最大項目：3 分。
- Summary 順序與防止外部修改：2 分。

---

## 第 3 題：繼承與薪資計算

配分：10 分  
指定檔名：`Q03_EmployeePayroll.java`  
指定類別：`Q03_EmployeePayroll`

### 必要結構

```java
public class Q03_EmployeePayroll {
    public static abstract class Employee {
        protected Employee(String id, String name)
        public String getId()
        public String getName()
        public abstract int monthlyPay();
        public String summary()
    }

    public static class SalariedEmployee extends Employee {
        public SalariedEmployee(String id, String name, int salary)
        @Override public int monthlyPay()
    }

    public static class HourlyEmployee extends Employee {
        public HourlyEmployee(String id, String name, int hours, int hourlyRate)
        @Override public int monthlyPay()
    }

    public static int totalPayroll(java.util.List<Employee> employees)
}
```

### 功能要求

1. Employee 的 `id`、`name` 不得為 `null` 或 blank，否則丟出 `IllegalArgumentException`。
2. `SalariedEmployee` 的 salary 小於 0 時以 0 計算。
3. `HourlyEmployee` 的 hours 與 hourlyRate 小於 0 時各以 0 計算。
4. HourlyEmployee 在 160 小時以內按原時薪計算；超過 160 小時的部分按 1.5 倍計算，最後轉為整數。
5. `summary()` 回傳 `id|name|monthlyPay`，必須透過 polymorphism 呼叫實際子類別的 `monthlyPay()`。
6. `totalPayroll()` 加總所有非 `null` Employee；List 為 `null` 時回傳 0。

### 範例

```java
var employees = java.util.List.of(
    new Q03_EmployeePayroll.SalariedEmployee("E1", "Amy", 50000),
    new Q03_EmployeePayroll.HourlyEmployee("E2", "Bo", 170, 200)
);
System.out.println(employees.get(0).summary());
System.out.println(employees.get(1).summary());
System.out.println(Q03_EmployeePayroll.totalPayroll(employees));
```

```text
E1|Amy|50000
E2|Bo|35000
85000
```


### 配分

- Abstract superclass 與 validation：2 分。
- 兩種 `monthlyPay()`：4 分。
- Override 與 `summary()`：2 分。
- Polymorphic total：2 分。

---

## 第 4 題：Interface 通知路由

配分：10 分  
指定檔名：`Q04_NotificationRouter.java`  
指定類別：`Q04_NotificationRouter`

### 必要結構

```java
public class Q04_NotificationRouter {
    public interface Channel {
        String name();
        boolean supports(String destination);
        String send(String destination, String message);
    }

    public static class EmailChannel implements Channel { /* ... */ }
    public static class SmsChannel implements Channel { /* ... */ }

    public static java.util.List<String> route(
        java.util.List<Channel> channels,
        String destination,
        String message
    )
}
```

### 規則

1. `EmailChannel.name()` 回傳 `EMAIL`，只支援包含 `@` 且 `@` 不在開頭或結尾的 destination。
2. `SmsChannel.name()` 回傳 `SMS`，只支援去除 `-` 後恰好 10 個數字的 destination。
3. `send()` 回傳 `CHANNEL|destination|message`，CHANNEL 是 `EMAIL` 或 `SMS`。
4. `route()` 依 channels 順序呼叫所有支援該 destination 的 channel，回傳 send 結果。
5. `channels`、destination 或 message 為 `null` 時回傳 empty List；List 中的 `null` channel 要略過。

### 範例

```java
var channels = java.util.List.of(
    new Q04_NotificationRouter.EmailChannel(),
    new Q04_NotificationRouter.SmsChannel()
);
System.out.println(Q04_NotificationRouter.route(channels, "a@b.com", "Ready"));
System.out.println(Q04_NotificationRouter.route(channels, "0912-345-678", "Go"));
```

```text
[EMAIL|a@b.com|Ready]
[SMS|0912-345-678|Go]
```


### 配分

- Interface 與兩個實作：3 分。
- Email/SMS validation：3 分。
- Polymorphic route：3 分。
- Null edge cases：1 分。

---

## 第 5 題：Generic 有界資料箱

配分：10 分  
指定檔名：`Q05_BoundedBox.java`  
指定類別：`Q05_BoundedBox<T extends Comparable<T>>`

### 必要 Method

```java
public Q05_BoundedBox(int capacity)
public boolean add(T value)
public int size()
public boolean isFull()
public T minimum()
public T maximum()
public int countGreaterThan(T threshold)
public java.util.List<T> snapshot()
```

### 功能要求

1. Capacity 小於 1 時丟出 `IllegalArgumentException`。
2. `add()` 拒絕 `null` 及超過容量的資料，回傳 `false` 且不修改內容。
3. `minimum()`、`maximum()` 在 empty box 回傳 `null`，其他情況使用 `compareTo()`。
4. `countGreaterThan()` 計算嚴格大於 threshold 的數量；threshold 為 `null` 時回傳 0。
5. `snapshot()` 保留加入順序，caller 修改回傳 List 時不能影響 box。

### 範例

```java
Q05_BoundedBox<Integer> box = new Q05_BoundedBox<>(3);
System.out.println(box.add(40));
System.out.println(box.add(10));
System.out.println(box.add(30));
System.out.println(box.add(20));
System.out.println(box.minimum());
System.out.println(box.maximum());
System.out.println(box.countGreaterThan(25));
System.out.println(box.snapshot());
```

```text
true
true
true
false
10
40
2
[40, 10, 30]
```


### 配分

- Generic bound、capacity 與 add：3 分。
- Minimum/maximum：3 分。
- Threshold count：2 分。
- Snapshot encapsulation：2 分。

---

## 第 6 題：Map 與 Set 選課索引

配分：10 分  
指定檔名：`Q06_EnrollmentIndex.java`  
指定類別：`Q06_EnrollmentIndex`

### 必要 Method

```java
public boolean enroll(String courseCode, String studentId)
public boolean drop(String courseCode, String studentId)
public int courseSize(String courseCode)
public java.util.List<String> studentsOf(String courseCode)
public java.util.List<String> coursesOf(String studentId)
public java.util.Map<String, Integer> summary()
```

### 規則

1. 類別內部使用 `Map<String, Set<String>>` 管理 courseCode 與 studentId。
2. Code 或 id 為 `null`、blank 時，`enroll()` 與 `drop()` 回傳 `false`。
3. 重複選課不增加資料並回傳 `false`。
4. Drop 成功後若該課程已無人選課，從 Map 移除該 courseCode。
5. `studentsOf()`、`coursesOf()` 與 `summary()` 都依字典順序輸出。
6. 所有查詢結果不得暴露內部 collection。

### 範例

```java
Q06_EnrollmentIndex index = new Q06_EnrollmentIndex();
index.enroll("DS", "S02");
index.enroll("DS", "S01");
index.enroll("JAVA", "S01");
System.out.println(index.studentsOf("DS"));
System.out.println(index.coursesOf("S01"));
System.out.println(index.summary());
```

```text
[S01, S02]
[DS, JAVA]
{DS=2, JAVA=1}
```


### 配分

- Map/Set 結構與 enroll：3 分。
- Drop 及空課程清理：2 分。
- 雙向查詢：3 分。
- 排序、summary 與 encapsulation：2 分。

---

## 第 7 題：Stack 與 Queue 請求流程

配分：10 分  
指定檔名：`Q07_RequestPipeline.java`  
指定類別：`Q07_RequestPipeline`

### 必要 Method

```java
public static boolean isBalanced(String text)
public static java.util.List<String> process(String[] commands)
```

### `isBalanced()` 規則

- 檢查 `()`、`[]`、`{}` 是否正確配對與巢狀。
- 其他字元忽略。
- `null` 回傳 `false`；空字串回傳 `true`。
- 必須使用 `Deque<Character>` 作為 Stack。

### `process()` 規則

每個 command 是下列其中之一：

- `NORMAL id`：加入一般 Queue 尾端。
- `URGENT id`：加入緊急 Queue 尾端。
- `PROCESS`：優先處理最早加入的緊急請求；若無緊急請求再處理一般請求；兩者皆空時加入 `EMPTY` 至結果。

空白 command、格式錯誤或 `null` command 必須忽略。`commands` 為 `null` 時回傳 empty List。必須使用 `Deque<String>` 實作兩個 Queue。

### 範例

```java
String[] commands = {
    "NORMAL N1", "URGENT U1", "NORMAL N2", "PROCESS", "PROCESS", "PROCESS"
};
System.out.println(Q07_RequestPipeline.isBalanced("a{b[c](d)}"));
System.out.println(Q07_RequestPipeline.isBalanced("([)]"));
System.out.println(Q07_RequestPipeline.process(commands));
```

```text
true
false
[U1, N1, N2]
```


### 配分

- Balanced stack：4 分。
- 兩個 queue 與 priority：4 分。
- Invalid/empty/null cases：2 分。

---

## 第 8 題：Recursive 資料稽核

配分：10 分  
指定檔名：`Q08_RecursiveAudit.java`  
指定類別：`Q08_RecursiveAudit`

### 必要 Method

```java
public static int sumValid(int[] data, int index)
public static int countOccurrences(int[] data, int index, int target)
public static boolean isPalindrome(String text, int left, int right)
```

### 規則

1. 三個 method 都必須使用 recursion，不可使用任何 loop、Stream 或額外 collection。
2. `sumValid()` 從 index 開始加總 0 到 100 的值；`data` 為 `null` 或 index 超過範圍時回傳 0；負數 index 從 0 開始。
3. `countOccurrences()` 從 index 開始計算 target 次數，null 與 index 規則同上。
4. `isPalindrome()` 忽略大小寫，但不忽略空白或標點；`text` 為 `null` 回傳 `false`；當 `left >= right` 回傳 `true`。

### 範例

```java
int[] data = {10, -1, 20, 101, 20};
System.out.println(Q08_RecursiveAudit.sumValid(data, 0));
System.out.println(Q08_RecursiveAudit.countOccurrences(data, 0, 20));
System.out.println(Q08_RecursiveAudit.isPalindrome("Level", 0, 4));
```

```text
50
2
true
```


### 配分

- Base case 與 index handling：3 分。
- Recursive valid sum：2 分。
- Recursive occurrence count：2 分。
- Recursive palindrome：3 分。

---

## 第 9 題：Binary Tree 四種走訪

配分：10 分  
指定檔名：`Q09_TreeTraversal.java`  
指定類別：`Q09_TreeTraversal`

### 必要結構

```java
public class Q09_TreeTraversal {
    public static class Node {
        public int value;
        public Node left;
        public Node right;
        public Node(int value)
    }

    public static java.util.List<Integer> preorder(Node root)
    public static java.util.List<Integer> inorder(Node root)
    public static java.util.List<Integer> postorder(Node root)
    public static java.util.List<Integer> levelOrder(Node root)
}
```

### 規則

1. 前三種走訪必須使用 recursion。
2. Level-order 必須使用 `Queue` 或 `Deque`。
3. Root 為 `null` 時回傳 empty List。
4. 每個 method 都要建立新的 result，不能共用會殘留前次結果的 static List。

### 測試樹

```text
        8
      /   \
     4     12
    / \      \
   2   6      14
```

### 正確結果

```text
preorder  = [8, 4, 2, 6, 12, 14]
inorder   = [2, 4, 6, 8, 12, 14]
postorder = [2, 6, 4, 14, 12, 8]
level     = [8, 4, 12, 2, 6, 14]
```


### 配分

- Preorder：2 分。
- Inorder：2 分。
- Postorder：2 分。
- Level-order：3 分。
- Empty 與重複呼叫：1 分。

---

## 第 10 題：BST 搜尋與插入

配分：10 分  
指定檔名：`Q10_BstDirectory.java`  
指定類別：`Q10_BstDirectory`

### 必要 Method

```java
public boolean add(int value)
public boolean contains(int value)
public int size()
public java.util.List<Integer> searchPath(int target)
public java.util.List<Integer> inorder()
public boolean isValid()
```

### 規則

1. 較小值放 left subtree，較大值放 right subtree。
2. Duplicate value 不加入並回傳 `false`。
3. `contains()` 找到回傳 `true`，找不到回傳 `false`。
4. `searchPath()` 收集實際比較過的 node value；找不到時不加入 `null`。
5. `inorder()` 回傳升冪資料；empty tree 回傳 empty List。
6. `isValid()` 必須使用 ancestor low/high boundary，不得只檢查直接 child。

### 範例

```java
Q10_BstDirectory tree = new Q10_BstDirectory();
for (int value : new int[]{50, 30, 70, 20, 40, 60, 80}) tree.add(value);
System.out.println(tree.add(40));
System.out.println(tree.searchPath(60));
System.out.println(tree.searchPath(65));
System.out.println(tree.inorder());
System.out.println(tree.isValid());
```

```text
false
[50, 70, 60]
[50, 70, 60]
[20, 30, 40, 50, 60, 70, 80]
true
```


### 配分

- Add 與 duplicate：3 分。
- Contains 與 path：3 分。
- Size 與 inorder：2 分。
- 全域 validation：2 分。

---

## 第 11 題：BST 三種刪除

配分：10 分  
指定檔名：`Q11_BstDeletion.java`  
指定類別：`Q11_BstDeletion`

### 必要 Method

```java
public boolean add(int value)
public boolean remove(int value)
public boolean contains(int value)
public int size()
public java.util.List<Integer> inorder()
public boolean isValid()
```

### 規則

1. Add 規則與一般不接受 duplicate 的 BST 相同。
2. `remove()` 找不到 target 時回傳 `false` 且 tree 不變。
3. 必須正確處理 leaf、one child、two children 與 root delete。
4. Two-child case 使用 right subtree minimum 作 inorder successor。
5. Remove 成功後 size 減 1，所有剩餘 node 仍可由 root 到達。
6. `isValid()` 使用全域 boundary。

### 範例

```java
Q11_BstDeletion tree = new Q11_BstDeletion();
for (int value : new int[]{50, 30, 70, 20, 40, 60, 80}) tree.add(value);
System.out.println(tree.remove(20));
System.out.println(tree.remove(30));
System.out.println(tree.remove(50));
System.out.println(tree.remove(999));
System.out.println(tree.inorder());
System.out.println(tree.size());
System.out.println(tree.isValid());
```

```text
true
true
true
false
[40, 60, 70, 80]
4
true
```


### 配分

- Leaf 與 missing：2 分。
- One-child delete：2 分。
- Two-child delete：3 分。
- Root、size、inorder 與 validation：3 分。

---

## 第 12 題：學籍 BST 綜合系統

配分：10 分  
指定檔名：`Q12_StudentBstSystem.java`  
指定類別：`Q12_StudentBstSystem`

### 必要結構

```java
public class Q12_StudentBstSystem {
    public static class Student {
        public Student(int id, String name, int score)
        public int getId()
        public String getName()
        public int getScore()
        public String toString()
    }

    public boolean add(Student student)
    public Student find(int id)
    public boolean updateScore(int id, int score)
    public boolean remove(int id)
    public java.util.List<Student> studentsBetween(int lowId, int highId)
    public java.util.List<Student> inorder()
}
```

### 規則

1. Student id 必須大於 0，name 不得為 `null` 或 blank；不合法時 constructor 丟出 `IllegalArgumentException`。
2. Score 小於 0 儲存為 0，大於 100 儲存為 100。
3. BST 以 id 作為唯一 key；`add(null)` 與 duplicate id 回傳 `false`。
4. `find()` 找不到回傳 `null`。
5. `updateScore()` 找到才更新，score 同樣限制在 0 到 100。
6. `remove()` 必須支援三種 delete case；two-child 使用 right subtree minimum。
7. `studentsBetween()` 包含 lowId 與 highId，依 id 升冪；lowId > highId 回傳 empty List。
8. `inorder()` 依 id 升冪回傳所有 Student。
9. `toString()` 格式為 `id|name|score`。

### 範例

```java
Q12_StudentBstSystem system = new Q12_StudentBstSystem();
system.add(new Q12_StudentBstSystem.Student(300, "Mina", 78));
system.add(new Q12_StudentBstSystem.Student(100, "Leo", 84));
system.add(new Q12_StudentBstSystem.Student(500, "Nora", 105));
system.add(new Q12_StudentBstSystem.Student(200, "Ivy", 69));
System.out.println(system.updateScore(200, 88));
System.out.println(system.studentsBetween(150, 500));
System.out.println(system.remove(300));
System.out.println(system.inorder());
```

```text
true
[200|Ivy|88, 300|Mina|78, 500|Nora|100]
true
[100|Leo|84, 200|Ivy|88, 500|Nora|100]
```


### 配分

- Student validation、encapsulation 與格式：2 分。
- Add、find、duplicate：2 分。
- Update 與 score boundary：2 分。
- 三種 remove case：2 分。
- Range query 與 inorder：2 分。

---

## 繳交前檢查

1. `midterm_exam` 中是否剛好有指定的 12 個 `.java` 檔案。
2. 每個檔案的 `public class` 是否與檔名一致。
3. 是否能在 JDK 17 使用 `javac *.java` 完成編譯。
4. 是否測試 empty、null、duplicate、missing 與 boundary value。
5. 是否把最新版本 push 到指定繳交位置。
